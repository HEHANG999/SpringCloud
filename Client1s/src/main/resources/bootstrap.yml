#设置本服务器端口号
server:
  port: 8003

#设置本服务器名称
spring:
  application:
    name: Client1

#设置本服务器id地址
eureka:
  instance:
    hostname: 127.0.0.1

#注册服务器的地址
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8000/eureka/
